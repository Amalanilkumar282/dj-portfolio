/**
 * Domain-aware compositions (TrackCard, EventCard, GigMap…).
 *
 * Empty for now: the cinematic components built in Phase 10 live in
 * `apps/web/src/components/` because they are app compositions wired to that
 * app's data layer, not reusable design-system parts. Anything that proves
 * genuinely reusable by `apps/admin` should be promoted here.
 *
 * This barrel exists so the `./composites` subpath in package.json resolves.
 */

export {};
